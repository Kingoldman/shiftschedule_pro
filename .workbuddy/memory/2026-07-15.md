# 2026-07-15 工作日志

## 项目代码审查（只读，未修改）
- 项目：隔壁小王爱值班（shiftschedule_pro），FastAPI + Vue3 全栈排班系统。
- 产出：`code-review-report.md`（位于项目根目录），完整审查报告。
- 严重问题（已逐字核对源码）：
  1. C1 JWT 默认密钥硬编码（`core/config.py:22`）→ Token 可伪造。
  2. C2 SQLite 外键级联未启用（`core/database.py` 无 `PRAGMA foreign_keys=ON`）→ 删员工留孤儿 state_log。
  3. C3 默认管理员 admin/admin123 不强制改密。
  4. C4 批量导入/排序接口用 `list[dict]` 零校验 → KeyError/ValueError 直接 500。
  5. C5 全部读接口无鉴权，PII 暴露。
  6. C6 `compute_eligible_days`（`services/schedule_service.py:168-209`）全员"应值班天数"相同且与分子口径不一致，频率失真；`EmployeeStateLog` 形同死代码。
- 前端高危：Token 明文存 localStorage、PDF 导出 innerHTML 注入、路由无导航守卫、auth store JSON.parse 无保护。
- 用户要求：只提意见建议，不要直接修改代码（已遵守，仅新增报告文件）。
